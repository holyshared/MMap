(function($){

var MMap = (this.MMap || {});

MMap.MarkerManager = new Class({

	Extends: MMap.Container,

	options: {
		markers: []
	},

	initialize: function(options) {
		this.parent(option.markers);
	}

});

}(document.id))